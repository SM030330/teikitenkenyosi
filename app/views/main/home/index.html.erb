<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Logo</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse show justify-content-between" id="navbarNavAltMarkup">    
      <div class="navbar-nav">
        <% if main_user_signed_in? %>
          <a class="nav-link active" aria-current="page" href="#">Home</a>
          <a class="nav-link" href="#">all-notice</a>
          <a class="nav-link" href="#">my-page</a>
          <a class="nav-link" href="#">cofig</a>
          <%= render 'main/home/create_inspection_modal' %>
        <% end %>
      </div>
      
      <button type="button" class="btn btn-outline-secondary">
        <% if main_user_signed_in? %>
          <%= link_to "Log out", destroy_main_user_session_path, method: :delete %>
        <% else %>
          <%= link_to "Log in", new_main_user_session_path %>
        <% end %>
      </button>
    </div>
    
  </div>
</nav>
<div class="alert alert-primary" role="alert">
  <%= flash[:notice] if flash[:notice].present? %>
</div>

